/// The maximal length of a string that an task can pass to the kernel.
pub const MAX_STR: usize = 256;

/// The maximal length of a path that an task can pass to the kernel.
pub const MAX_PATH: usize = 256;

/// The length of buffers used to transfer data
pub const BUFFERED_LEN: usize = 256;
